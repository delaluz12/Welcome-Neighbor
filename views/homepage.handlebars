{{!-- homepage html --}}
{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{user.role_id}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}

{{!-- maria's drafted handlebar homepage container --}}
<div class="container d-grid gap-3 mb-5">

  <div class="row justify-content-center">
    <div class="col-xxl-3 col-md-5 col-sm-6 col-lg-4 col-9">
      <img src="/css/assets/images/logo.png" alt="" srcset="">
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-xxl-9 col-md-11 col-sm-9 col-lg-8 col-10 ">
      <h1 class="text-center" style="color: #175c4a;">Connect with your Neighbors in Our town</h1>
    </div>
  </div>

  <div class="row justify-content-center">
    {{#each posts as |post|}}
    {{> globalPost}}
    {{/each}}
    
  </div>

  <div class="row justify-content-center mb-5">
    <div class=" col-xxl-3 col-md-5 col-lg-4 col-sm-5 col-9 align-self-start">
      <a href="/browse"><button type="button" class="btn btn-primary">Browse Neighborhoods</button></a>
    </div>
  </div>

</div>